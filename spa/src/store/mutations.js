// import _ from 'underscore'
// import Vue from 'vue'

export default {
  SET_CARDS (state, cards) {
    state.cards = cards
  },
  SET_USER_INFO (state, user_info) {
    state.user_info = user_info
  }
}
